spring:
 datasource:
  url: jdbc:mysql://localhost:3307/db_feedback?useSSL=false&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true
  username: root
  password: root
 jpa:
  hibernate:
   ddl-auto: update
 web:
  error:
   include-stacktrace: on-param